/**
 * Demo configuration and credentials.
 * Gated with React Native's global __DEV__ flag.
 */
export const IS_DEV = __DEV__;

export const DEMO_CREDENTIALS = {
  username: 'emilys',
  password: 'emilyspass',
};
